# =============================================================================
# API - Variables de entorno
# =============================================================================
# Mismas variables para local y producción. Copia a .env.local (desarrollo)
# y a .env.production (producción); rellena los valores según cada entorno.
# =============================================================================

# Puerto del servidor

PORT=4000

# MongoDB - URI de conexión

# Local: mongodb://localhost:27017/kds_db
# Production: mongodb+srv://db_user:db_password@cluster_url/db_name?appName=app-name
MONGODB_URI=mongodb://localhost:27017/kds_db

# Opcionales (por defecto: maxPoolSize=10, serverSelectionTimeoutMS=5000, socketTimeoutMS=45000)

# MONGODB_MAX_POOL_SIZE=10
# MONGODB_SERVER_SELECTION_TIMEOUT_MS=5000
# MONGODB_SOCKET_TIMEOUT_MS=45000
